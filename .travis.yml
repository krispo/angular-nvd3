language: node_js

node_js:
  - "0.10"

branch:
  only:
    - master

script:
  - node_modules/.bin/karma start test/karma.conf.js --auto-watch --single-run --reporters dots,coverage --browsers PhantomJS

